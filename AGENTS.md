# AGENTS.md — sc-inter (온라인 미니 축구)

이 파일이 1순위 원본이다. 세부 절차는 `docs/` 로 위임한다.

## 1. 프로젝트 한 줄

브라우저에서 두 사람이 인터넷을 통해 3대3(각 팀 사람 1명 + AI 동료 2명)으로 즐기는 3D 축구 게임. 실제 사람 비율의 선수가 드리블·질주·개인기·패스를 쓴다. 서버가 경기 상태의 유일한 권위(authoritative)이고, 브라우저는 입력 전송과 렌더링만 한다.

## 2. 구조

```
app/api/src   Node + TypeScript. WebSocket 실시간 서버 + 정적 웹 서빙(동일 origin)
app/web/src   React + TypeScript + Vite + Three.js. 로비 화면과 3D 경기 화면
docs/         사람과 AI의 공동 SSOT (한글 폴더·파일명)
```

루트에는 문서와 메타 파일만 둔다.

## 3. 규칙

- `docs/` 와 코드가 충돌하면 코드를 고치기 전에 멈추고 사람에게 확인한다.
- 변경 루프는 READ → CODE → (필요시) DOC → 항상 HANDOFF.
- 문서·설명은 한국어, 코드 식별자와 경로는 영어.
- 시크릿 값은 코드·문서·로그·예제에 남기지 않는다. 변수 이름까지만 적는다.
- TypeScript strict 유지. 서버가 받는 모든 메시지는 경계에서 검증한다.
- 치트·디버그용 상태 조작 엔드포인트를 공개하지 않는다. 공개 HTTP 면은 정적 파일과 `/healthz` 뿐이다.

## 4. 승인 경계

- 로컬 실행·테스트·의존성 설치·커밋·`origin` push: 승인됨.
- Git 조작(stage/commit/push)은 통합 담당 한 사람만 수행한다.
- 실제 배포, 공개 터널(ngrok 등), 운영 환경 변경: 사람의 별도 승인 전에는 실행하지 않는다.

## 5. 문서 지도

| 문서 | 내용 |
| --- | --- |
| `docs/기획/게임규칙.md` | 경기 규칙과 조작 |
| `docs/백엔드/실시간계약.md` | WebSocket 메시지 계약 |
| `docs/백엔드/시뮬레이션.md` | 물리·틱·권위 모델 |
| `docs/프론트엔드/화면.md` | 화면 구성과 디자인 기준 |
| `docs/운영/실행과배포.md` | 로컬 실행, 환경변수, 배포 준비 |
| `docs/핸드오프/` | 작업 인계 기록 |
