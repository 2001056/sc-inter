# HANDOFF

최신 인계 기록은 [`docs/핸드오프/202609/`](docs/핸드오프/202609/) 에 날짜별로 쌓는다.
이 파일은 "지금 이 저장소를 처음 여는 사람이 알아야 할 것"만 요약한다.

## 지금 상태 (2026-09-19 갱신)

| 영역 | 상태 |
| --- | --- |
| `app/api` (서버) | 3대3 권위 시뮬레이션·WebSocket·정적 서빙·health·graceful shutdown. 2026-09-19 에 AI 판단 고도화, 서버 권위 경기 기록 `snapshot.stats`, 패스 재소유 유예(0.28초)와 킥오프권 추가. 서버 테스트 119개 통과 |
| `app/web` (3D 화면) | 로비·3D 경기 화면. 2026-09-19 에 그래픽 품질(자동/고화질/성능)과 렌더 비용 절감, 경기 알림·득점 자막·효과음·경기 기록, 입력 전송 분리, 작은 화면 HUD 추가. 웹 테스트 44개 통과 |
| 통합 | 단일 프로세스(8787)로 웹+WebSocket 서빙. `81442bd` 기준 typecheck·build exit 0, API 119/119, web 44/44, 실제 8787 두 페이지 확인(아래) |
| 운영 방침 | **로컬 전용**(2026-09-19 사용자 선택). 서버는 `HOST=127.0.0.1` 루프백에만 바인드하고 공개 터널·배포는 하지 않는다 |
| 배포 구성 | `Dockerfile`, `compose.yaml` 준비. 정식 공개는 **미승인**. 다만 2026-09-18 에 공개 터널이 한 번 의도치 않게 열렸다가 종료됐다(아래 사고 항목) |
| 저장소 | `github.com/2001056/sc-inter` `main` 에 기능 단위로 push |

2026-09-19 작업 기록(날짜별 파일):
[API·AI·경기기록](docs/핸드오프/202609/2026-09-19-API-AI고도화와-경기기록.md) ·
[패스 유예·킥오프권](docs/핸드오프/202609/2026-09-19-패스릴리스와-킥오프.md) ·
[렌더링](docs/핸드오프/202609/2026-09-19-렌더링-고도화.md) ·
[경기 경험(UI)](docs/핸드오프/202609/2026-09-19-경기경험-고도화.md)

## 2026-09-19 "더 고도화" 결과

사용자는 이번 단계에서 **로컬 전용**을 골랐다. 공개 터널·배포·운영 접속은 하지 않았다.

### 새로 들어간 것

| 영역 | 내용 |
| --- | --- |
| AI·경기 | AI 판단 고도화, `snapshot.stats`(슛·패스·성공 패스·점유 시간), 패스한 선수의 즉시 재소유 막기, 킥오프권(첫 경기 추첨·재경기 교대·실점 팀) |
| 렌더 | 그래픽 품질 `auto`/`high`/`low`(로비·경기 설정에서 선택, 저장소 막혀도 동작), 그림자 맵 축소·메시 병합(draw call 99 → 81), 카메라 앞 공간·패스 대상 표시 |
| 경기 UI | 슛·패스·개인기·킥오프 알림, 중계 자막식 득점 연출, 경기 중 `기록` 판과 종료 비교표, `설정` 판 |
| 효과음 | Web Audio 합성. 기본 꺼짐, 사용자 동작에서만 켬, 끄기·탭 숨김 때 예약된 소리까지 멈춤, 화면 이탈 때 닫음 |
| 입력 | 30Hz 입력을 렌더 루프와 분리한 독립 타이머. 조작 키는 그대로(방향키·S·D·E·Shift+방향·Q·C·V) |
| 작은 화면 | 폭 720px 이하·높이 520px 이하 가로에서 점수판 한 줄·가로 도구줄·게이지 2×2. 가로 휴대폰의 게이지-조이스틱 겹침 해소 |

### 커밋 (모두 `origin/main` 에 push)

| 커밋 | 내용 |
| --- | --- |
| `4f8e7df` | feat(api): AI 판단 고도화와 서버 권위 경기 기록(snapshot.stats) |
| `6f81143` | fix(api): 패스한 선수의 즉시 재소유 막기와 킥오프권 도입 |
| `1d587de` | feat(web): 그래픽 품질 선택과 렌더 비용 절감 |
| `81442bd` | feat(web): 경기 알림·득점 자막·효과음·경기 기록과 입력 전송 분리 |

중간 커밋 `6f81143`·`1d587de` 도 단독으로 풀어 typecheck·API 테스트(119)·해당 web 테스트가 통과하는 것을 확인했다.

### 검증 (2026-09-19, 통합 담당)

- `pnpm typecheck` exit 0, `pnpm build` exit 0, `pnpm test`(API) 119/119, `pnpm --filter @soccer/web test` 44/44.
- **실제 8787**(최신 빌드, `127.0.0.1` 바인드)에 Playwright 로 두 페이지를 띄워 확인했다(로컬 Chrome 헤드리스):
  - 방 만들기(왼쪽) → 코드로 참가(오른쪽), 선수 6명(3:3), 양쪽 점수판에 두 닉네임.
  - 같은 순간 두 페이지의 서버 tick·남은 시간·점수 일치.
  - 키: `↑`(ax>0, 오른쪽 자리에서는 ax<0), `E` 달리기, `D` 누름 동안 shoot, `S` 패스 1회, `Shift+←` 개인기 방향 1회, `Q` 태클 1회, `C` 조작 선수 전환(L2→L3 실제 반영), `V` 는 서버로 동작을 보내지 않음. seq 단조 증가.
  - 창 이탈(blur) 즉시 중립 입력.
  - `snapshot.stats` 가 실제 WS 에 실리고 `기록` 판 값이 그 값과 같음.
  - 효과음: 켜기 전 AudioContext 생성 0 → 설정에서 켤 때 생성 → 숨김 suspend → 보임 resume → 끄기 suspend → 나가기 close.
  - `localStorage` 가 모두 예외를 던지는 페이지에서도 로비 그래픽 선택·연습 경기·효과음·품질 전환이 오류 없이 동작.
  - 게스트 새로고침 → 같은 방·같은 자리(right)로 복귀, 호스트에 `opponentDisconnected` → `opponentReconnected`, 복귀한 쪽에 지난 알림·자막 없음.
  - 페이지 오류 0건.
- 경기 종료 기록과 재경기 0 초기화는 API 테스트(`app/api/test/ai-stats.test.ts` 의 "재경기에서만 초기화", "방의 재경기 흐름에서도 기록이 초기화") 와 UI 작업의 스크래치 서버 150초 완주 재현으로 확인했다. 8787 에서 150초 완주는 반복하지 않았다. 치트·디버그 엔드포인트는 추가하지 않았다.

### 지금 떠 있는 서버

```bash
# 기존 PID 3195(0.0.0.0:8787, connections 0 확인)를 SIGTERM 으로 정상 종료한 뒤
PORT=8787 HOST=127.0.0.1 nohup node app/api/dist/index.js > logs/server-8787.log 2>&1 &
# PID 84247 → logs/server-8787.pid (logs/ 는 .gitignore)
curl -s http://127.0.0.1:8787/healthz
```

에이전트 셸에서 `rtk proxy` 를 거쳐 띄웠기 때문에 node 의 부모가 `rtk` 래퍼 프로세스다. 중지할 때는
운영 문서대로 PID 파일의 PID 만 `ps` 로 확인하고 종료한다.

### 캡처

[`docs/핸드오프/202609/스크린샷/`](docs/핸드오프/202609/스크린샷/) 의 `0919-*.jpg`:
두 브라우저 호스트(기록 판)·게스트, 모바일 세로·가로 경기 화면, 득점 자막, 종료 기록 비교.
득점 자막과 종료 비교 두 장은 UI 작업 때 스크래치 서버에서 찍은 것이고, 나머지는 최신 소스 기준이다.

### 이번 단계의 한계

- **실제 사용자 기기의 fps, 실제 터치 기기, 효과음의 실제 청감은 확인하지 않았다.** 모든 브라우저 확인은 이 맥북의 헤드리스 Chrome 이다.
- 입력 독립 타이머는 렌더(GPU)가 느릴 때 30Hz 를 지키지만, 자바스크립트 메인 스레드가 막히면 전송 간격도 같이 늘어난다(UI 인계 기록의 대조 측정 참고).
- 킥오프 직후 화면의 입력 활성은 보간 지연과 10Hz 반영 때문에 서버보다 약간 늦다. 그 틈에 누르기 시작한 키는 무시되며, 실제 키보드는 자동 반복으로 곧 복구되지만 자동화처럼 반복이 없는 입력은 복구되지 않는다(기존 동작, 이번에 바꾸지 않음).
- 킥오프권을 받은 팀을 화면에 따로 표시하지 않는다. 상대가 킥오프 순간 공으로 달려드는 것은 막지 않는다.
- 두 브라우저 대전에서 알림·득점 자막이 양쪽에 같게 뜨는지는 득점 장면까지 가지 않아 확인하지 않았다.

## 먼저 읽을 것

1. [`AGENTS.md`](AGENTS.md) — 규칙과 승인 경계
2. [`docs/기획/게임규칙.md`](docs/기획/게임규칙.md) — 무엇을 만드는 게임인지
3. [`docs/백엔드/실시간계약.md`](docs/백엔드/실시간계약.md) — 서버·프런트 공동 계약(원본 타입은 `app/api/src/protocol.ts`)
4. [`docs/운영/실행과배포.md`](docs/운영/실행과배포.md) — 실행·개발 서버 관리·배포 준비

## 빨리 돌려보기

```bash
pnpm install
pnpm build
HOST=127.0.0.1 PORT=8787 pnpm start     # http://127.0.0.1:8787 (로컬 전용)
```

## 남은 배포 단계 (사람 승인 필요 — 아직 실행 안 함)

지금 사용자 선택은 로컬 전용이다. 아래는 나중에 공개를 따로 승인받을 때를 위한 기록이다.

1. **임시 공개**: Cloudflare quick tunnel 한 줄이면 되고, 넘기는 대상은 이 게임의 8787 포트 하나다. 도구는 설치해 두었다(2026.9.1). 정확한 명령과 주의점은 운영 문서에 있다.
2. **또는 컨테이너 배포**: `docker build -t sc-inter . && docker run -p 8787:8787 sc-inter` → `/healthz` 확인
3. 리버스 프록시를 쓸 경우 WebSocket 업그레이드 헤더(`Upgrade`, `Connection`) 통과 설정
4. 공개 주소로 두 브라우저 최종 확인

자세한 내용과 주의점은 [`docs/운영/실행과배포.md`](docs/운영/실행과배포.md) 에 있다.

## 사고: 승인 전에 공개 터널이 열림 (2026-09-18)

셸 인용 실수로 공개 터널 명령이 실제 실행되어 **00:59:25Z 부터 01:01:31Z 까지 약 2분 6초** 동안 임시 공개 주소가 살아 있었다. 승인은 없었다. 넘어간 대상은 로컬 8787 포트 하나이고, 종료 후 프로세스는 남아 있지 않으며 그 주소는 서버에 닿지 않는다. **그 사이 외부에서 접속했는지는 서버에 요청 로그가 없어 알 수 없다.**

경위·사실관계·재발 방지는 [`docs/핸드오프/202609/2026-09-18-백엔드와-통합.md`](docs/핸드오프/202609/2026-09-18-백엔드와-통합.md) 의 사고 기록에 적었다. 정식 공개는 여전히 사람의 승인을 기다린다.

## 아직 재보지 않은 것: 실제 기기 성능

2026-09-19 렌더 작업은 합성 장면에서 SwiftShader·M1 Max 두 환경만 쟀다(렌더 인계 기록). 아래 9/18 의 느린 프레임 원인은 여전히 확정하지 않았다.

별도 렌더 스모크에서 화면은 정상적으로 그려지고 충전 게이지도 0 → 100% → 0 으로 정확히 움직였다(JS 오류 0건). 다만 그 헤드리스 환경의 프레임 진행은 초당 11회 수준으로, 빈 페이지 대조(초당 60회)보다 뚜렷하게 느렸다.

실제 사용자 기기에서 몇 fps 가 나오는지, GPU 가속이 걸리는지, 느려진 원인이 무엇인지는 **확인되지 않았다.** 60fps 가 나온다거나, GPU 문제라거나, 자동화 환경 탓이라고 단정할 근거가 아직 없다. 실제 기기에서 프레임을 재보는 것이 남은 과제다.
