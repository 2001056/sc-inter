# 2026-09-19 API — 패스 재소유 유예와 킥오프권

작업자: Opus 워커(dispatch `ctx_b723c17281c3`, task `task_13b3767503b1`). 기준은 `main` `4f8e7df` 위의 작업 트리.
요청: 독립 검토 보고서(`/tmp/sc-inter-advanced-review/report.md`)에서 확정된 F1·F2 만 고친다. F3·F4 튜닝과 UI·렌더·프로토콜·root HANDOFF·Git·8787 은 건드리지 않았다.

## 소유 파일(이번 변경)

| 파일 | 내용 |
| --- | --- |
| `app/api/src/game/constants.ts` | `PASS.releaseMs = 280`, `KICKOFF`(`takerGap 0.95`, `takerRole forward`), `kickoffSpotFor()` 추가. `spawnFor` 는 값 그대로(주석만 "기본 대형") |
| `app/api/src/game/sim.ts` | `Player.passReleaseMs`, `findController` 후보 제외, `Match.kickoffSide`·`openingKickoff`, `startMatch` 추첨/교대, 득점 시 실점 팀 지정, `resetPositions` 킥오프 배치, `resetToSpawn(spot?)` |
| `app/api/test/kickoff-release.test.ts` | 새 테스트 19개 |
| `app/api/test/team-play.test.ts` | 두 테스트의 기대 위치를 `spawnFor` → `kickoffSpotFor(…, kickoffSide)` 로 바꾸고, 득점 뒤 `kickoffSide === "right"` 단언 추가 |
| `docs/기획/게임규칙.md` | 킥오프권 규칙과 패스 재소유 유예 |
| `docs/백엔드/시뮬레이션.md` | 볼 컨트롤 유예, `## 킥오프권` 절, 테스트 목록 |

## F1 패스 재소유 유예

- 공을 실제로 찬 패스(킥 사거리 안, 받을 동료가 없는 패스 포함)에만 `passReleaseMs = 280` 을 건다. 헛발질은 `passCdMs` 만 걸리고 유예는 0 이다.
- 유예 중인 선수는 `findController` 후보에서 빠진다. 선수-공 충돌·`touch`·마지막 터치·패스 통계·조작 전환은 바뀌지 않았다. 슛·이동 물리도 그대로다.
- ai.ts 는 수정하지 않았다(패스한 선수를 지원 자리로 보내는 분기는 그대로).

## F2 킥오프권

- 첫 경기는 `Math.random() < 0.5` 로 추첨하고, 재경기는 직전 경기 첫 킥오프 팀과 번갈아 간다. 득점 뒤에는 실점한 팀이 찬다.
- 킥오프권 팀 forward 는 `(28 ∓ 0.95, 17)`, 나머지 다섯 명은 기본 대형에 선다. 상대 forward 는 공에서 4.32m 떨어져 있다.
- 프로토콜과 WS 필드는 바꾸지 않았다. 킥오프권은 스냅샷의 선수 위치로 보인다. 컨트롤 중인 사람 선수는 여전히 L3/R3(forward)라서 킥오프권이 있으면 사람이 바로 공 뒤에서 시작한다.

## 검증

- `npx tsc --noEmit`: 통과(0).
- `npm test`(app/api): **119/119 통과**(기존 100 + 새 19). 처음 돌렸을 때 기존 테스트 2개가 새 킥오프 배치 때문에 실패했고, 위 표처럼 기대값을 고쳤다.
- 뮤테이션 확인(`/tmp/sc-inter-advanced-review/mut` 사본): `findController` 제외 줄을 지우면 새 테스트 2개가 실패하고, 킥오프 배치를 기본 대형으로 되돌리면 7개가 실패한다. 테스트가 실제로 결함을 잡는다는 뜻이다.
- 하네스 전후 비교(`/tmp/sc-inter-advanced-review`, AI 대 AI):

| | 4f8e7df | 이번 변경 |
| --- | --- | --- |
| 패스 뒤 패스한 선수 재소유(10경기) | 624/648(96%), 평균 19.8틱, 최대 228틱 | **0/673** |
| 킥오프 뒤 첫 소유 L:R(20경기) | 91:0 | 56:52 |
| 왼쪽 점유율(20경기) | 0.563±0.021 | 0.488±0.018 |
| 10경기 골 L/R | 20/22 | 24/22 |

## 한계

- 수치는 모두 AI 대 AI 헤드리스 시뮬레이션이다. 브라우저, 사람 조작, 8787 서버에서는 확인하지 않았다(8787 갱신은 통합 담당 몫).
- 킥오프권은 배치로만 보장한다. 상대가 킥오프 순간 공으로 달려드는 것은 막지 않는다(센터서클 규칙 없음). 사람이 킥오프권을 받고도 가만히 있으면 상대가 4.3m 를 달려와 가져갈 수 있다.
- 상대에게 킥오프권 팀이 누구인지 알려 주는 UI 표시는 없다(요청 범위 밖).
- ai.ts 의 패스한 선수 분기, F3(헛발질), F4(군집)는 손대지 않았다.
- Git 스테이징과 커밋은 하지 않았다. 후속 통합 담당이 위 소유 파일만 골라 커밋해야 한다(같은 트리에 UI 워커의 변경이 있다).

## 검토 보고서 정정

`/tmp/sc-inter-advanced-review/report.md` 의 F1 표에서 b867632 와 4f8e7df 행이 뒤바뀌어 있었다(결과 파일 `pdold.txt`/`pdnew.txt` 를 다시 확인함). 올바른 값은 다음과 같다.
b867632: 841 패스 중 565(67%), 평균 15.1틱, 1초 이상 19건, 최대 368틱.
4f8e7df: 1248 패스 중 1207(97%), 평균 19.6틱, 1초 이상 40건, 최대 228틱.
즉 4f8e7df 의 AI 변경은 재소유를 줄인 것이 아니라 늘렸다. 이번 수정으로 0이 됐다.
