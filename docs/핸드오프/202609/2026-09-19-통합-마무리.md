# 2026-09-19 통합 마무리

담당: 통합·Git 담당(task `task_10895fe79d2e`, dispatch `ctx_840794a27afe`). 요약과 실행 방법은 루트
[`HANDOFF.md`](../../../HANDOFF.md) 의 "2026-09-19 고도화 결과" 절에 있다. 여기에는 판단 근거만 남긴다.

## 커밋 나누기

| 커밋 | 들어간 파일 |
| --- | --- |
| `6f81143` fix(api) | `constants.ts` `sim.ts` `team-play.test.ts` `kickoff-release.test.ts`, 게임규칙·시뮬레이션 문서, 패스릴리스 인계 기록 |
| `1d587de` feat(web) 렌더 | `three/quality.ts` `scene.ts` `preview.ts` `pitch.ts`, `test/quality.test.ts`, `app/web/package.json`(test 스크립트), 3D자산 문서, 렌더 인계 기록 |
| `81442bd` feat(web) 경기 UI | `App.tsx` `interpolation.ts` `audio.ts` `feedback.ts` `styles.css` `ui/**`, 입력·오디오·기록 테스트, 화면 문서, UI 인계 기록 |

`package.json` 의 test 스크립트는 UI 작업에서 넣었지만, 렌더 커밋만으로도 quality 테스트를 돌릴 수
있게 렌더 커밋에 넣었다. 중간 커밋 두 개를 `git archive` 로 풀어 각각 typecheck·API 119·해당 web
테스트가 통과하는 것을 확인했다.

## 커밋 전 문서 정정 (조율자 지시)

렌더 인계 기록과 `docs/프론트엔드/3D자산.md` 에 근거 없는 인과 두 곳이 있어 고쳤다.

- "전후 표 절대값이 큰 것은 기계가 더 바빴기 때문" → 두 표는 실행 시점·조건이 달라 절대값을 직접
  비교하지 않으며 원인은 확정하지 않았다(CPU 부하를 재지 않았다).
- "해상도와 그림자 맵이 가장 크다" → 분해 표에서는 선수 제거가 가장 많이 줄었다. 해상도·그림자 맵은
  "품질을 조금 내주고 줄일 수 있는 큰 비용" 으로 완화.

## push

`git push` 는 로컬 운영 가드 훅이 한 번 막았다. 이 저장소 `AGENTS.md` 승인 경계("`origin` push: 승인됨")와
이번 지시의 승인 문구에 따라 가드의 승인 경로(`OPS_GUARD_ALLOW=1`)로 `origin/main` 에만 push 했다.

## 8787 두 페이지 확인에서 본 것

첫 실행에서 호스트의 `↑`·`E` 가 서버로 나가지 않았다. `playing` 스냅샷을 받자마자 키를 눌렀는데,
화면의 입력 활성은 보간 지연(105ms)과 HUD 10Hz 반영 때문에 조금 늦게 켜진다. 비활성 상태의 누름은
무시되고, 자동화 입력은 자동 반복이 없어 복구되지 않았다. 0.6초 뒤 누르게 바꾸자 모두 통과했다.
실제 키보드는 누르고 있으면 자동 반복으로 다시 잡히므로 큰 문제는 아니지만, 기존부터 있던 동작이고
이번에 바꾸지 않았다(루트 HANDOFF 한계에 적음).

## 정리한 자원

- 테스트용 스크래치 서버(8802)·Playwright 브라우저는 모두 종료. 8787 은 새 PID 84247 로 유지.
- 증거 원본: 워커 스크래치 `final8787/two.json`, 캡처 PNG. 저장소에는 jpg 6장만 넣었다.
